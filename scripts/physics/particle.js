
/**
 * 
 */
class Particle {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }

    /**
     * Progress the particle simulation by [delta].
     * @param {number} delta 
     */
    step(delta) {
        
    }
}

export class Particle { }
