---
# Quiz data in JSON format, generated from wiki pages
---

[
{% capture newline %}
{% endcapture %}
  {% assign cards = site.pages | where: "layout", "wiki" %}
  {% for card in cards %}
    {
      "question": "{{ card.title | escape }}",
      "answer": "{{ card.description | replace: newline, " " | escape }}",
      "category": "{{ card.dir }}"
    }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
